/**
 * Auto-loaded extension shim — auto-retry fallback audit log.
 * See ./specsafe-session.ts for why these shims exist (Seshat v2 slice v2.0).
 */
export { default } from "../hooks/fallback-audit";
